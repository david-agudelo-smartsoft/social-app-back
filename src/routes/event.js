const express = require('express');
const router = express.Router();
// const { getPublications, getPublication, createPublication, updatePublication, deletePublication } = require('../controller/PublicationController');

// router.route('/').get(getPublications).post(createPublication);

// router.route('/:id').get(getPublication).put(updatePublication).delete(deletePublication);

// module.exports = router;